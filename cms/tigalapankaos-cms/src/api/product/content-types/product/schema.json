{
  "kind": "collectionType",
  "collectionName": "products",
  "info": {
    "singularName": "product",
    "pluralName": "products",
    "displayName": "Product",
    "description": "Produk kaos yang ditampilkan di website"
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "name": { "type": "string", "required": true },
    "slug": { "type": "uid", "targetField": "name", "required": true },
    "category": {
      "type": "enumeration",
      "enum": ["unisex", "men", "women"],
      "required": true,
      "default": "unisex"
    },
    "price": { "type": "integer", "required": true },
    "material": { "type": "string" },
    "description": { "type": "text" },
    "sizes": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::size.size",
      "inversedBy": "products",
      "description": "Tinggal pilih (select) ukuran yang tersedia, tidak perlu ketik manual"
    },
    "images": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"],
      "description": "Foto utama/cover produk (opsional jika sudah ada foto per varian warna)"
    },
    "variants": {
      "type": "component",
      "repeatable": true,
      "component": "product.color-variant",
      "description": "Sub-produk per warna: setiap warna punya foto & stok sendiri. Tambah baris baru untuk tiap warna."
    },
    "featured": { "type": "boolean", "default": false },
    "stockQuantity": {
      "type": "integer",
      "default": 0,
      "min": 0,
      "description": "Total stok produk ini. Kalau produk punya varian warna, isi total stok gabungan semua warna di sini, dan isi juga stok per warna di bagian Varian Warna."
    },
    "stockAvailable": { "type": "boolean", "default": true }
  }
}
