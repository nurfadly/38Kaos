/**
 * Seed data awal (produk, artikel, cabang) supaya CMS tidak kosong
 * saat pertama kali deploy. Jalankan setelah strapi start dengan:
 *   npm run seed
 * (Strapi harus sedang berjalan / atau jalankan via strapi console jika perlu)
 */
console.log(
  'Gunakan Strapi Admin Panel (Content Manager) untuk menambahkan data pertama kali,\n' +
  'atau import lewat Strapi Transfer / Import-Export plugin dari data JSON di /scripts/seed-data.json'
);
